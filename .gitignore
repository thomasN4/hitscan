# No trailing slash: a trailing slash restricts the pattern to DIRECTORIES,
# and git treats a symlink as a file. A `node_modules -> ../acsc-main/node_modules`
# symlink (the way parallel worktrees share one install) therefore slipped past
# `node_modules/` and was committed to main, so every fresh clone got a dangling
# link. Keep these bare.
node_modules
dist

# The review job writes the model's markdown here before posting it
# (.github/workflows/review.yml), and AGENTS.md documents running the same
# command locally — so without this, a `git add -A` commits a review into the
# repo.
review.md

# Plan Relay keeps approved plan copies, OpenCode JSONL transcripts and the
# per-run summary.json here. They are local execution records, not source
# artifacts, and they are routinely pruned — the committed record is the
# planner's wave entry in docs/plan-relay-log.md, which cites a run directory by
# name and has to survive that directory going away.
.plan-relay

# Blender sources are committed; rotating backups and temporary renders are not.
*.blend1
*.blend2
assets/previews/
